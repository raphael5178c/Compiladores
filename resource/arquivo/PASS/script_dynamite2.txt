program Teste2;
var x, y, z : integer;

procedure p;
	var A : integer;
	begin
		readln( a );
		if a = x then 
			z := z + x
		else begin
			z := z - x;	
			call p;
		end;
	end;

begin
	Z := 0;
	readln( x, y );
	if x > y then
		call p
	else 
		Z := z + x + y;
	writeln( z );
end.