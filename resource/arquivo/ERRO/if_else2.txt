Program ifelse;
	Var x1, x2, x3, media: Integer;
Begin
	writeln("Informe três valores.");
	Readln(x1);
	Readln(x2);
	Readln(x3);
	media := (x1 + x2 + x3) / 3;
	if media > 7 then
		begin
			writeln("Aprovado!");
		end
	else
	begin
		writeln("Reprovado!")
	end;
	writeln("Média: ", media);
End.